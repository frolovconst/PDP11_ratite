#include"definitions.h"
#include"ratite_mem_funcs.h"
#ifndef RATITE_INSTRUCTIONS_H
#define RATITE_INSTRUCTIONS_H

bool mov(byte [MEM_SIZE], int , int ) ;
bool sob(byte [MEM_SIZE], byte, byte );
bool add(byte [MEM_SIZE], int , int );

#endif // RATITE_INSTRUCTIONS_H
